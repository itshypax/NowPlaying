name: Release builds

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to build and attach the binaries to (e.g. v0.1.0)"
        required: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - name: Build standalone binaries
        run: |
          set -e
          for target in windows-x64 linux-x64 darwin-x64 darwin-arm64; do
            dir="dist/nowplaying-$target"
            mkdir -p "$dir"
            out="$dir/nowplaying"
            if [ "$target" = "windows-x64" ]; then out="$out.exe"; fi
            bun build --compile --target="bun-$target" server.ts --outfile "$out"
            cp -r public "$dir/"
            cp README.md LICENSE "$dir/"
            (cd dist && zip -qr "nowplaying-$target.zip" "nowplaying-$target")
          done
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag || github.ref_name }}
          files: dist/*.zip
